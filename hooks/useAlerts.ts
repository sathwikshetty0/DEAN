'use client';

/**
 * @fileoverview Utility module for useAlerts
 * Implements functionality related to the D-EAN platform's core logic layer.
 */

export { useAlerts } from '@/context/AlertContext';
